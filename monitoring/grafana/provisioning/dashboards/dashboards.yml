apiVersion: 1
providers:
  - name: FinTechDemo
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # Docker volume：./monitoring/grafana/dashboards → /var/lib/grafana/dashboards
      # （勿寫宿主機 D:/… 路徑，容器內讀不到）
      path: /var/lib/grafana/dashboards
