# FinTechDemo — 總驗收清單（loop-engineering）

> 對齊權威規格：[FinTechDemo-SPEC.md](../FinTechDemo-SPEC.md)  
> 驗證入口：`.\scripts\check.ps1` · `.\scripts\verify-pipeline.ps1`  
> 最後整理：2026-08-07

---

## 1. 自動化驗證（必過）

| # | 指令 | 通過標準 |
|---|------|----------|
| A1 | `.\scripts\check.ps1` | 結尾 `FinTechDemo check OK`；`BUILD SUCCESSFUL` |
| A2 | `.\scripts\verify-pipeline.ps1` | 結尾 `PIPELINE_OK`（check + compose config + k8s overlay） |
| A3 | `cd frontend; npm run build` | Vite build 成功（exit 0） |
| A4（建議） | `.\gradlew.bat check --rerun-tasks` | 強制重跑後仍綠（避免 UP-TO-DATE 漏測） |

可選現場煙霧（需 Docker）：

```powershell
.\scripts\verify-pipeline.ps1 -Up -Smoke
```

---

## 2. 最短可成交 Demo（人工）

| # | 步驟 | 預期 |
|---|------|------|
| B1 | 起 `RiskServiceApplication` :8082 | `/actuator/health` UP |
| B2 | 起 `OrderServiceApplication` :8081 | health／Swagger 可開 |
| B3 | `cd frontend; npm run dev` → http://localhost:5173/login | 登入頁可開 |
| B4 | `trader1` / `password` 登入 | 進交易前台 |
| B5 | 下單 → PENDING | 訂單出現 |
| B6 | 點「成交」 | Risk Feign 通過 → `ACCEPTED`；關 Risk 再開成交應失敗可見 |
| B7 | `/blueprint` | 技術棧表／Mermaid 圖可渲染（可不登入） |
| B8 | PROCESS FLOW（Trade 右側） | 有 demoTrace／拓撲燈（Peers 需起來才綠） |

一鍵最短後端：`.\scripts\start-demo.ps1 -StartMinimal`

---

## 3. 文件對齊

| # | 文件 | 狀態檢查 |
|---|------|----------|
| C1 | `FinTechDemo-SPEC.md` §2.3 | 固定 URL Feign＋Eureka 升級金句 |
| C2 | `docs/architecture/系統運作藍圖.md` + 圖檔 `images/*.png` | 與 SPA `/blueprint` 同敘事 |
| C3 | `docs/測試與CI.md` · `docs/測試規格書.md` | check／fixture／壓測／觀測 |
| C4 | `README.md` | 10 分鐘啟動、藍圖、驗證入口 |
| C5 | `docs/_md/guides/spec.md`／fusion | 與 SPEC 同步；HTML 經 `generate-docs-html` |

---

## 4. 註解（comment_verbosity: detailed）

| # | 範圍 | 標準 |
|---|------|------|
| D1 | public 型別 | 有【職責】；教學點補【技巧】【概念】 |
| D2 | Feign Client | 註明**固定 URL**；概念可提 Eureka／`lb://` 升級（非本版必做） |
| D3 | 測試 | Case 行為／Given-When-Then 可讀 |

權威：`EngineeringOS/eos-minimal/knowledge/comments.md`

---

## 5. 服務發現（評量口徑，勿現場硬改主線）

本版：**固定 URL Feign** 先跑穩交易鏈。

升級口頭（見 SPEC §2.3）：

> 拿掉 `url`、改服務名＋Eureka，Gateway 改 `lb://`——**發現機制升級，不是重寫業務**。

完整 Eureka 串接練習倉：**TradingMicroService**（APIGatewayMQ **無** Eureka）。

---

## 6. 簽核

| 項目 | 結果 | 日期 |
|------|------|------|
| A1–A3 自動化 | ☐ | |
| B1–B8 最短成交 | ☐ | |
| C1–C5 文件 | ☐ | |
| 推送 GitHub `main` | ☐ | |
