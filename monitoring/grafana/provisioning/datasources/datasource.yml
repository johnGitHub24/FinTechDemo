apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # Docker Compose 服務名（同 network）；本機免 Docker 請用 start-monitoring-local.ps1
    url: http://prometheus:9090
    isDefault: true
    editable: false
