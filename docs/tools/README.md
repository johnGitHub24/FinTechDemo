# docs/tools/ — 文件工具（非 Pure）

Canonical: `EngineeringOS/.../optional-demo-scripts/docs-tools/`  
Sync: `apply-workspace.ps1 -WithDemo`

| File | Role |
|------|------|
| `serve-docs.ps1` / `serve_docs_http.py` | `:5500` |
| `generate-docs-html.ps1` / `.mjs` / `package.json` | `_md` → HTML |

```powershell
.\docs\tools\serve-docs.ps1
.\docs\tools\generate-docs-html.ps1
```
